use "../../src/structs/arena.npk".{TCBArena, arena_init, tcb_arena_allocate}; use "../../src/structs/handle.npk".IdxHandle; use "../../src/structs/tcb.npk".TCB; pub func:main = int32() { TCBArena: arena; drop arena_init(@arena, 10i64); Result<IdxHandle<TCB> >: rh = tcb_arena_allocate(@arena); if (rh.is_error) { exit 1i64; } exit 0i64; }; pub func:failsafe = int32(tbb32:err) { exit 99i64; };
