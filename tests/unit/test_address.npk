pub struct:BigStruct = {
    int64: a; int64: b; int64: c;
};
pub struct:Slot = {
    BigStruct: item;
    int64: gen;
};
pub struct:Arena = {
    Slot->: slots;
};
pub func:main = int32() {
    Arena: arena;
    arena.slots = calloc(10i64, 32i64) => Slot->;
    BigStruct->: ptr = @(arena.slots[5i64].item);
    if (ptr == NULL) { exit 1i64; }
    exit 0i64;
};
pub func:failsafe = int32(tbb32:err) { exit 99i64; };
